name = "reviews"
main = "src/index.js"
compatibility_date = "2025-04-01"

# Tu cuenta de Cloudflare (de Luna)
account_id = "9cfdfc511c06601a2b669916d2ddb73b"

# KV namespace for storing reviews
kv_namespaces = [
  { binding = "REVIEWS_KV", id = "REPLACE_ME" }
]

# Admin token for moderation — set via `npx wrangler secret put ADMIN_TOKEN`
# with a secure random value, e.g. `openssl rand -hex 16`

# CORS: allowed origin for the landing page
[vars]
ALLOWED_ORIGINS = "https://bytewizzardcreators.github.io,https://byte-wizzard.xyz"
